name: Keep Hugging Face Space Awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Space
        run: curl -fsS https://huggingface.co/spaces/saqibahmadsiddiqui/hf-portfolio || true